# 短线事件驱动配置
# 策略参数
event_params:
  min_zt_count: 5      # 最小涨停家数触发热点板块
  lookback_days: 20    # 回看历史天数
  min_industry_stocks: 10  # 行业最小股票数量

# 数据缓存 (统一输出目录)
cache:
  dir: "outputs/shortterm/cache"
  expire_hours: 4

# 输出配置 (统一输出目录)
output:
  signals_file: "outputs/shortterm/technical_overview/latest.json"
  history_file: "outputs/shortterm/history/sector_heat_history.csv"
  database_file: "outputs/shortterm/database/signals.db"
  charts_dir: "outputs/shortterm/charts"

# 分析参数
analysis:
  correlation_window: 10   # 相关性计算窗口
  win_rate_threshold: 0.6  # 胜率阈值
  avg_return_threshold: 0.005  # 平均收益阈值
